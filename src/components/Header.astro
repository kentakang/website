---
const currentPath = Astro.url.pathname;
const menus = [
  { path: '/', label: 'About' },
  { path: '/articles', label: 'Articles' },
];
const defaultPathClass = 'text-lg font-medium';
const activePathClass = `${defaultPathClass} font-bold text-primary`;
---
<div class="flex gap-6 items-center">
  <img src="/images/profile.jpg" alt="kentakang profile" class="w-16 h-16 rounded-sm" />
  <p class="text-4xl font-bold">kentakang</p>
</div>
<div class="flex gap-4 mt-4 mb-8">
  {menus.map((menu) => (
    <a href={menu.path} class={menu.path === currentPath ? activePathClass : defaultPathClass}>{menu.label}</a>
  ))}
</div>